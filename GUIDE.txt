# Руководство пользователя: Архиватор v2.0

Это руководство поможет установить, настроить и использовать программу резервного копирования важных данных. 
Поддерживаются Windows 10/11 и Windows Server 2012 и новее (x86/x64, см. примечания по архитектуре ниже).

## 1. Возможности
- Резервное копирование файловой базы или папки с важными данными в ZIP-архив
- Копирование архива на дополнительный диск/каталог
- Расписание (ежедневно, каждый час, каждые 6/12 часов, несколько триггеров)
- Email-уведомления об успешном/ошибочном бэкапе
- Настройка автозапуска задач через Планировщик заданий Windows
- По желанию пользователя: закрытие активных процессов 1С перед архивацией (если вы используете программу для архивации 1С рекомендуется  оставить включенной)

## 2. Требования
- Windows (рекомендуется Windows 10/11 или Windows Server 2012 и новее)
- Права на чтение каталога и запись на диски для архива и копии
- Для email уведомлений: доступ к SMTP-серверу, корректные учетные данные
- Последняя версия (для вашей системы) Microsoft Visual C++ Redistributable

## 3. Установка
1) Скачайте установщик для вашей платформы (Win10/11 x86/x64 или Server x86/x64)
2) Запустите установщик и следуйте инструкциям
3) После установки запустите «Archiver» из меню Пуск

Примечание по архитектуре: используйте x64 на 64-битной ОС. Для 32-битных систем используйте x86.

## 4. Первый запуск
При первом запуске откроется главное окно с тремя вкладками:
- Основные настройки
- Расписание
- Email уведомления

## 5. Настройка конфигурации
На вкладке «Основные настройки»:
1) БД или папка:
   - Поле «Путь»: выберите каталог необходимый для архивации (кнопка «Обзор»)
   - «Имя конфигурации»: укажите уникальное имя (например, «БД_Продакшн»)
2) Диски для архивации:
   - «Диск для хранения архивов»: каталог, где будет создаваться ZIP-архив
   - «Диск для копии архивов»: второй каталог для копии архива (Крайне рекомендуется выделять место на отдельном диске)
3) Опция закрытия процессов 1С:
   - Чекбокс «Закрывать активные процессы 1С перед архивацией»
   - Включите, если хотите, чтобы программа перед бэкапом закрывала процессы 1С (`1cv8.exe`, `1cv8c.exe`)
   - Рекомендуется при файловой базе, чтобы избежать «занятости» файлов

Нажмите «💾 Сохранить конфигурацию» — запись появится в таблице «Сохранённые конфигурации».

## 6. Расписание
На вкладке «Расписание» добавляйте один или несколько триггеров:
- Кнопка «+ Добавить триггер» добавляет новое время и интервал (ежедневно/каждый час/каждые 6/12 часов)
- Удаляйте лишние триггеры кнопкой ✕ справа

Список триггеров сохраняется в конфигурацию при нажатии «💾 Сохранить конфигурацию» на главной вкладке.

## 7. Автозапуск (Планировщик заданий)
На вкладке «Основные настройки» в группе «Сохранённые конфигурации» доступны кнопки:
- «🔍 Проверить автозапуск»: показывает, создана ли задача для текущей конфигурации
- «⚙ Настроить автозапуск»: создаёт задачи в Планировщике по добавленным триггерам
- «❌ Удалить автозапуск»: удаляет созданные задачи

## 8. Email уведомления
На вкладке «Email уведомления» заполните параметры SMTP и получателей:
- Включите отправку, укажите сервер, порт, логин/пароль, отправителя и получателей
- Отметьте, какие события слать: об успехе и/или об ошибке

Примечание: глобальные настройки применяются ко всем конфигурациям. Внутри программы настройки сохраняются и валидируются.

## 9. Ручной запуск
- Выберите конфигурацию в таблице (двойной клик — подгружает значения в поля)
- Нажмите «▶ Запустить архивацию»
- При включённом чекбоксе «Закрывать активные процессы 1С…» программа сначала мягко завершит процессы 1С, затем при необходимости завершит принудительно
- По завершении появится сообщение о результате; при включённых email-уведомлениях отправится письмо

## 10. Где хранятся данные
- Конфигурации хранятся в локальной базе `backup_configs.db` в каталоге данных приложения:
  - Windows: `%LOCALAPPDATA%\Backup1C`
- Логи работы находятся в корневой папке программы

## 11. Советы и лучшие практики
- Для файловых баз 1С рекомендуется включать опцию закрытия процессов 1С, чтобы снять блокировки файлов
- Размещайте «Диск для копии архивов» на другом физическом носителе
- Регулярно проверяйте доступность путей и свободное место
- Периодически делайте тестовое восстановление из архива

## 12. Устранение неполадок
- «Путь не существует» при запуске: проверьте, что все каталоги доступны и существуют
- Архив не создаётся: убедитесь, что файлы базы не заняты (включите чекбокс закрытия 1С) и хватает прав/места
- Не создаётся задача в Планировщике: запустите программу от имени администратора и повторите
- Email не уходит: проверьте SMTP-параметры, логин/пароль, SSL/порт; смотрите журнал ошибок

## 13. Частые вопросы (FAQ)
В: Можно ли иметь несколько расписаний на одну конфигурацию?
О: Да, добавьте несколько триггеров на вкладке «Расписание».

В: Что делает опция «Закрывать активные процессы 1С»?
О: Перед архивацией программа завершит процессы `1cv8.exe`/`1cv8c.exe` (сначала мягко, затем при необходимости принудительно).

В: Где посмотреть логи?
О: В корневом каталоге данных приложения 

## 15. Примечания по версиям
- Win10/11 x86/x64 и Server x86/x64 содержат одинаковый функционал
- Интерфейс и опции (включая чекбокс закрытия процессов 1С) доступны во всех версиях

## 16. Поддержка
При обращении укажите:
- Версию Windows и разрядность
- Версию сборки Архиватора
- Текст ошибки (скриншот/лог) и шаги воспроизведения

Для вопросов, предложениях и сообщениях об ошибках обращаться на почту zip@file-py.ru

Мы в ВК https://vk.com/urban_solution?from=groups

Мы в телеграме https://t.me/+3PdeknJPJRc4NGNi


